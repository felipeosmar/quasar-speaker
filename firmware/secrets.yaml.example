wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
api_key: "GENERATE_WITH: python3 -c 'import secrets,base64;print(base64.b64encode(secrets.token_bytes(32)).decode())'"
ota_password: "quasar2026"
